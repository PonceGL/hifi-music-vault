#!/bin/sh
set -e

echo "[pre-commit] Running lint..."
pnpm lint

echo "[pre-commit] Running format check..."
pnpm format:check

echo "[pre-commit] Running test:coverage..."
pnpm test:coverage

echo "[pre-commit] Running build..."
pnpm build

echo "[pre-commit] All checks passed."
